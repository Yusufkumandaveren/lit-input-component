name: Build and Publish WebJar to GitHub Packages

on:
  release:
    types: [published]

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Repo’yu çek
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Java kur
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          server-id: github
          settings-path: ${{ github.workspace }}

      # 3️⃣ WebJar build
      - name: Build WebJar
        run: |
          cd webjar
          mvn -B clean package

      # 4️⃣ GitHub Packages’a deploy
      - name: Publish to GitHub Packages
        run: |
          cd webjar
          mvn -B deploy -s $GITHUB_WORKSPACE/settings.xml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
